# Handoff — resume after the four-hour break

Superseded for next execution by [the Claude Code handoff](HANDOFF-CLAUDE-CODE.md).
The user now explicitly authorizes Claude Code to perform the next bounded
task and create its own commit, overriding the older exclusion below.

Continuation status: the bounded task below has been implemented in
`Polygon/TimeSubdivision.lean`. See [the result and next obligation](TIME_SUBDIVISION.md),
STATE.md and VERIFICATION.md for the updated state. The original task text
below is retained as its scope and stopping rule, not as a fresh queue item.

## User intent and corrections (authoritative)

Formalize the arguments corresponding to Book I, Section II, Propositions
I–IV in **De Motu, 1687 and 1713**, separately. Use only each stage's Newtonian
definitions, laws and invoked mathematics. Proposed 1694 and 1726 support
comparison; they are not the three main targets. Read GOALS.md and SECTION_II.md.

The active issue is **the area between an impulsive polygon and the eventual
trajectory**, not the area swept about the force centre. Earlier swept-area
counterexamples are valid but secondary; do not continue that detour.

For the forward polygon argument, trajectory existence must be established
alongside its required properties. Assuming an actual curve in order to bound
the polygon–curve defect leaves the central obligation open. Start with two
finite constructed polygons. Keep existence, partition independence, area
properties and mechanical force interpretation as separate obligations.
Proposition II may begin with a given curve because its converse statement
has a different premise.

**No results from integral calculus.** No imported integration, fundamental
theorem, ODE existence, measure theory or later analytic theorem may close a
historical proof. Finite Euclidean constructions and arithmetic are allowed;
coordinate verification must be labelled as our reconstruction.

The motivating hypothesis is that narrowing the failures may reveal an
action-valued boundary object and eventually a fixed universal nonzero action
constant. This is unproved. Do not assume the desired constant, infer it from
a failed tactic, or suppress successful classical repairs. Finite polygon
joining now works in the checked discrete construction.

Work in **short steps**: one bounded result, verification, brief report.

## Resource policy

Updated by the user's v6 instruction: use `gpt-6-sol` for bounded reasoning
and technical implementation, `gpt-6-luna` for routine checking/compilation.
This supersedes the earlier Terra and 5.5/5.6 assignments; do not silently
fall back to v5. The earlier main-process preference was Astra medium;
delegation policy does not change the main model.
Explicit authorization exists for these subagents. Run at most one subagent
at a time, sequentially; no Astra subagents or Claude. Delegate checking and
compilation, request concise results, and avoid redundant full audits.
Do not promise automatic resumption or silently change the main model.

## Current repository state

Working directory: `/home/codexssh/newtonlean`.

- `e2e656d`: approved goals, three-stage source map, finite contact/restart.
- `362c99f`: finite polygon-refinement strip, no assumed curve.
- This handoff is added in a subsequent documentation commit; verify live HEAD.
- Preserve unrelated deletion `conversation-continue-planck-gap-analysis.md`
  and untracked `continue-planck-gap-analysis.md`. Do not stage or restore them.
- Lean 4.19.0, core/Std only, empty external packages. No downloads needed now.

Read AGENTS.md, research/STATE.md, research/TASKS.md, this handoff, then only
the relevant Lean modules. The old M1-only boundary has been superseded by
the approved goals, but M1–M4 are still not certified complete.

## Checked results and their limits

`NewtonLimitDynamics/Polygon/Contact.lean`:

- `motion_restart`: exact recurrence restart from the current vertex pair
  and shifted impulses.
- Constructed lattice velocity-jump and zero-impulse contact results.
- Two inward impulse histories (-1 and -2) can give equal swept sums and
  different next vertices. This is not fixed-force nonuniqueness and is not
  the active defect-area problem.

`NewtonLimitDynamics/Polygon/RefinementStrip.lean`:

- For coarse edge A–C and fine polygon A–B–C, defines
  `refinementStripTwice = det A B + det B C - det A C`.
- `refinementStripTwice_eq_triangle` identifies this with
  `det (B-A) (C-A)`; translation invariance is proved.
- `refinementDefect` is the natural-number absolute value of this **doubled**
  signed triangle area. Zero iff signed strip zero is proved for one triangle.
  For multiple cells, absolute value of a total signed sum can hide cancellation;
  do not infer a positive total-error bound without additional work.
- `oneCellMotionRefinementCompatible` states shared spatial endpoints.
  Its start/end theorems project that assumption; they do not derive mechanical
  refinement. The coarse side is the initial cell (index zero), so its impulse
  argument does not influence the cell. Do not call this a common-force test.
- A concrete compatible example uses an inward fine impulse and has doubled
  defect 1. No continuous curve, time subdivision or convergence is proved.

`Contact/Bounds.lean`, `Contact/AreaCoefficient.lean` and
`Contact/FiniteSums.lean` contain conditional bounds and finite-sum arithmetic.
Their assumed enclosures cannot supply the missing curve. `Comparison/Routes`
has a polynomial example, not a derivation of trajectory existence.

## Next bounded task

Make **time and mechanical data explicit in one coarse-versus-fine comparison**.
The current endpoint condition is only assumed. Select and state a finite
impulse scheduling convention, positive coarse duration and two positive fine
durations with the same total, initial position/velocity, and matching stated
force data. Build both polygonal motions from those inputs using finite
arithmetic. A constant-force consistency example is acceptable if labelled
separately from Newton's general central-force theorem.

Prove whether the coarse/fine endpoints coincide. Do not force coincidence by
choosing initial data separately or assume that time refinement preserves old
vertices: many approximation schemes do not. If endpoints differ, derive the
endpoint mismatch explicitly and define any closing connector before claiming
an enclosed defect area. An informative failure of exact nesting is a valid
small result.

Use an arithmetic domain that can express the stated subdivisions; the present
integer lattice cannot model arbitrary halving. Reuse the rational interface
if needed without importing analysis. Stop after this one constructed
comparison and its precise compatibility/mismatch theorem. Then update state,
ledger and next obligation, validate, commit only scoped files, and report.

Later work (not to claim in this small step): finite sums of absolute strip
defects, position bounds, compatible families or appropriately controlled
non-nested families, and a justified limiting time-to-position map. Vanishing
area alone must not be declared sufficient for trajectory existence.

## Source findings to retain

SECTION_II.md contains exact anchors and edition-local dependencies.
De Motu antecedents for I and IV are identified in NATP00089 and NATP00090;
II/III counterparts were not found in the inspected ranges, a bounded absence.
1687 IV cites II and Lemmas V/XI. 1713 IV cites II, I corollaries 2/4 and
Lemma VII. NATP00089 revision-label ambiguities remain ungraphed.

## Verification at last code commit

Both `lake build` and `lake build NewtonLimitDynamics` passed. Formal catalogue:
97 declarations; graph: 77 nodes, 68 edges, 234 passages, 94 Lean references.
`lake env lean research/CheckReferences.lean` passed, with standard Lean logical
axioms only and no `sorry`. Graph/reference generation and whitespace checks
passed. Earlier unchanged-source validation covered 231 TEI anchors and all
22 archive hashes. See VERIFICATION.md for which checks were rerun.

For new code: update scripts/catalogue_formal.py's correspondence table when
adding a module, regenerate the ledger and references with the existing
scripts, and run both build targets plus reference/axiom checks. Delegate these
sequentially. Do not redownload sources or rerun manuscript audits for a finite
arithmetic change.
